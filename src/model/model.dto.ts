export interface IModel {
    model_avatar: string;
    full_name: string;
    description: string;
    average_raiting: number;
    count_of_reviews: number;
}

export interface IPricePerHour {
    min: number;
    max: number;
}